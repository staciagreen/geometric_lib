name: Python application

on:
  push:
    branches: [ "lab_5" ]
#  pull_request:
#    branches: [ "main" ]

jobs:
  test:
    strategy:
      matrix:
        os: [ windows-latest, ubuntu-latest ]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Run unittests
        run: |
          python -m unittest
