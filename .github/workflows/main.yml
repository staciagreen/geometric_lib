name: Python application

on:
  push:
    branches: [ "lab_5" ]
#  pull_request:
#    branches: [ "main" ]

jobs:
  test_windows:
    runs-on: windows-latest

    steps:
      - name: Run unittests
        run: |
          python -m unittest

  test_ubuntu:
    runs-on: ubuntu-latest

    steps:
      - name: Run unittests
        run: |
          python -m unittest

